kind: Route
apiVersion: route.openshift.io/v1
metadata:
    name: ${INST_NAME}
spec:
  to:
    kind: Service
    name: ${INST_NAME}-bridge-service
    weight: 100
  port:
    targetPort: rest-api
  wildcardPolicy: None