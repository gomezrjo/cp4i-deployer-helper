description: '${DEST_DESCRIPTION}'
config_type: 'webhook'
is_enabled: true
webhook:
  header_params: { ${DEST_WHP_VALUE} }
  method: 'POST'
  target: '${DESTINATION_URL}'
title: '${DESTINATION_NAME}'
