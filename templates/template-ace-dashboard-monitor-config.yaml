kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: ${INST_NAME}-${INST_NS}-cr-monitoring-view
subjects:
  - kind: ServiceAccount
    name: ${INST_NAME}-dash
    namespace: ${INST_NS}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-monitoring-view