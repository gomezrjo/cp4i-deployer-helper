{
    "client_id": "${CLIENT_ID}",
    "client_secret": "${CLIENT_SECRET}",
    "grant_type": "password",
    "password": "${APIC_ADMIN_PWD}",
    "realm": "${APIC_REALM}",
    "username": "${APIC_ADMIN_USER}"
}